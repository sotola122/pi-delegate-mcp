## Visual evidence addendum

When images are in scope, ground every visual claim in what the model received via `@` or `read`.

Add `## Visual evidence` beneath the base profile result with:

- Image path(s) inspected
- What is visible
- Severity, confidence, and failure scenario when reporting defects

Preserve the selected base output contract. Convert BMP to PNG before attach/read.
